# Mobile App (Pixel Face Agent)

Expo / React Native project lives here.

Setup sketch:
```
cd apps/mobile
npx create-expo-app@latest face-agent --template
```

Target features:
- Face-to-face assistant UI (voice + camera overlay)
- Secure auth → Supabase backend
- Background sync + push notifications
- Device owner launcher integration
