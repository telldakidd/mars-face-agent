# Backend / Automation Stack

Components to build here:
- Supabase schema + SQL migrations
- Edge functions / Next API routes for MT5, TradingView, Polymarket connectors
- n8n workflow definitions (export JSON)
- Task queue + worker scripts

Start with a `/schema` folder for SQL and `/functions` for serverless handlers.
