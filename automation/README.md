# Automation Workflows

Use this folder for n8n exports, cron specs, and monitoring SOPs.

Suggested subfolders:
- `n8n/` – exported JSON workflows
- `cron/` – `openclaw cron` specs for MarketFeed, RiskOps, Reporter
- `docs/` – incident response + alert routing
